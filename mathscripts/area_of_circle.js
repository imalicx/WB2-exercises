// area

var radiusOfCircle = 20;
var areaOfCircle = radiusOfCircle ** 2;
var pieOfCircle = 3.14;

// calculation
var totalDue = areaOfCircle * pieOfCircle;

// output
console.log("The area of a circle with radius " + radiusOfCircle + " is " + totalDue + ".");
