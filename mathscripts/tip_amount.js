// food amount

var foodCost = 777.77;
var tipPercent = 20;

// calculations
var tipAmount = (foodCost * tipPercent) / 100;
var totalAmount = foodCost + tipAmount;

// amount

console.log("The tip on a $" + foodCost + " food bill is $" + tipAmount + ".");